# -----------------------------------------------------------------------------
# SERVICE ACCOUNT
#
# An identity that pods use to talk to the Kubernetes API — distinct from a
# human user account. This is the "who" that Role/RoleBinding will grant
# permissions to.
# -----------------------------------------------------------------------------

apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-reader-sa
  namespace: default
